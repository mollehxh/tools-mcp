version = 2
status = "pending"
required_before = "release"
endpoint_kind = "temporary_ngrok_https"
client = "ChatGPT developer mode / Scan Tools"
expected_tools = [
  "exec_command",
  "write_stdin",
  "apply_patch",
  "skills.list",
  "skills.read",
]
observed_at = ""
notes = "Protocol v2 removes skills.install. Automated Inspector and conformance do not substitute for a fresh ChatGPT Developer Mode scan; this pending manual checkpoint blocks release signoff, not automated U7 verification."
